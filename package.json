{
  "name": "acme-13",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "author": "mhm13dev",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r run start",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
